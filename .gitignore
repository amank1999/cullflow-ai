# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust workspace (root now, since src-tauri and cullflow-core share one target dir)
/target

# Bundled ffmpeg sidecar binaries - fetched by scripts/fetch-ffmpeg.(sh|ps1)
# or by CI before building, never committed (large, platform-specific, and
# not ours to redistribute via git).
/src-tauri/binaries/*
!/src-tauri/binaries/.gitkeep

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
